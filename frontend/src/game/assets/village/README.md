# Village Placeholder Assets

The Phaser scene currently draws the generated coloured geometry directly so
the village works without an artwork pipeline. This directory is reserved for
the generated structure sprites and keeps the asset contract stable for the
future art pass.
