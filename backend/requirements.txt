# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Cache / matchmaking / pub-sub / cooldowns (SADD 2.4)
redis[hiredis]==5.2.0

# Settings & validation
pydantic-settings==2.6.1
pydantic[email]==2.10.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Realtime
websockets==13.1

# HTTP client (judge adapters)
httpx==0.27.2

# Resilience (SADD 4.4 — RetryPolicy: exponential backoff + jitter)
tenacity==9.0.0

# Structured logging with correlation ids (SADD 10.1)
structlog==24.4.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
respx==0.21.1
